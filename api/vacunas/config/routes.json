{
  "routes": [
    {
      "method": "GET",
      "path": "/vacunas",
      "handler": "vacunas.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vacunas/count",
      "handler": "vacunas.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vacunas/:id",
      "handler": "vacunas.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/vacunas",
      "handler": "vacunas.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/vacunas/:id",
      "handler": "vacunas.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/vacunas/:id",
      "handler": "vacunas.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
